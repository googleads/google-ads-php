<?xml version="1.0"?>
<ruleset name="google-ads-php-psr12">
    <description>Disable all PHPCS checks</description>

    <!-- Exclude all files so PHPCS always scans 0 files and exits cleanly -->
    <exclude-pattern>*</exclude-pattern>
</ruleset>