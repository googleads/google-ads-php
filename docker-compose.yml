version: '2'
services:
  web:
    # Points to the official, secure KTCB-managed Artifact Registry paths
    image: us-central1-docker.pkg.dev/google.com/kokoro-container-bakery/google-ads-php/php${PHP_VERSION:-8.1}-base:manual
    volumes:
      - ./:/google-ads-php
      - ~/:/root